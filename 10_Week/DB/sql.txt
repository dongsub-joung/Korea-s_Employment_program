-- 테이블 생성
CTEATE TABLE{
  PSEQ NUMBER(10) NOT NULL, PRIMARY KEY,
  NAME VARCHAER2(20) NOT NULL, 
  KIND VARCHAER2(20) NOT NULL,
  PRICE1 NUMBER(10) NOT NULL,
  PRICE2 NUMBER(10) NOT NULL,
  CONTENT VARCHAR2(1000),
  USEYN CHAR(1) DEFAULT 'y',
  PEGDATE DATE DEFAULT SYSDATE
};

-- 추가  
INSERT INTO CATEGORY (KIND, NAME, DETAL)
VALUES(10, '의류, '모든의류제품'0)

-- 테이블 명: 별칭, 컬럼 AS 별칭  
SELECT D.DEPARTMENT_ID AS DEPTNO FROM DEPARTMENT D

-- sql 순서  

-- GROUP BY

-- 중복 제거
SELECT DISTINCT KIND FROM PRODUCT;

-- 조건절 FIRST_NAME D로 시작되는 이름, 직급, 부서번호
SELECT FIRST_NAME, JOB_ID, DEPARTMENT_ID FROM EMPLOYEES EHERE FIRST_NAME LIKE 'Da%';
-- LIKE '%v%; ,  LIKE '%v';

-- WHERE
SELECT * FROM PRODUCT WHERE KIND=10;

-- OR WHERE NOT ST;
SELECT FIRST_NAME, SALARY, JOB_ID, DEPARTMENT_ID FROM EMPLOYEES WHERE SALARY>1000;

-- 사칙연산
SELECT NAME, PRICE, PRICE1-PRICE2 FROM PRODUCT;

-- COUNT() not include NULL value
SELECT COUNT(DEPARTMENT_ID) FROM EMPLOYEE;


